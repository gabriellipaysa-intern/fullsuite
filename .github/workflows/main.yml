name: My First Workflow

# 1. The Trigger
on: [push]

# 2. The Job
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a greeting
        run: echo "Hello! The workflow worked."